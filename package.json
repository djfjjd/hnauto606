{
  "name": "hana-parking-renewal",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test",
    "lint": "node --check src/main.js && node --check src/validation.js && node --check 'functions/api/[[path]].js'"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  }
}
